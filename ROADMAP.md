# Дорожня карта проєкту «Резерв ID»

> Документ описує **повну логіку, архітектуру та невирішені задачі** проєкту, щоб будь-який агент / розробник міг продовжити роботу без повторного занурення у контекст.

---

## 1. Призначення додатку

**Резерв ID** — автономний (offline-first) Expo React Native додаток-аналог застосунку «Резерв+» Міністерства оборони України. Він:

1. Відображає електронну військово-облікову картку («Резерв ID») із фото, ПІБ, статусом, датою народження та відстрочкою.
2. Має зворотній бік картки з QR-кодом, що дійсний до конкретної дати.
3. Дозволяє користувачу редагувати **усі** поля профілю локально через `Settings` екран та редагування на самій картці.
4. Зберігає увесь профіль у `AsyncStorage` (ключ `rezerv_profile_v1`) — **жодних backend-викликів**.
5. Генерує PDF «Військово-обліковий документ» строго за шаблоном `eVOD_2026_05_30_18_46_04.pdf`, підставляючи дані з локального профілю.
6. Має 4 таби: **Резерв ID • Сервіси • Вакансії • Меню**.
7. Підтримує екрани: Сповіщення, Сервіси (Редагувати дані онлайн), Вакансії (Підтримка), Меню (Сесії, Підтримка, Налаштування).

---

## 2. Технологічний стек

| Шар | Технологія |
|---|---|
| Framework | Expo SDK (Expo Router file-based routing) |
| UI | React Native + `react-native-reanimated` + `expo-image` |
| Іконки/шрифти | Inter (500/600/700/900), локальні asset PNG |
| Сховище | `AsyncStorage` через тонку обгортку `@/src/utils/storage` |
| QR | `react-native-qrcode-svg` (UI) + `qrcode` (PDF) |
| PDF | `expo-print` + `expo-sharing` |
| Фото | `expo-image-picker` (base64) |
| Шрифти | `@expo-google-fonts/inter` |

Backend (`/app/backend`) у MVP **не використовується** — він залишається для майбутніх інтеграцій.

---

## 3. Структура коду

```
/app/frontend
├── app/                          # Expo Router маршрути
│   ├── _layout.tsx               # Кореневий layout (Stack + SafeArea + fonts)
│   ├── index.tsx                 # ГОЛОВНИЙ екран: Резерв ID + таби
│   └── settings.tsx              # Налаштування профілю (повний редактор)
├── src/
│   ├── components/
│   │   ├── ServicesScreen.tsx
│   │   ├── EditOnlineScreen.tsx
│   │   ├── VacanciesScreen.tsx
│   │   ├── SupportScreen.tsx
│   │   ├── MenuScreen.tsx
│   │   ├── ActiveSessionsScreen.tsx
│   │   ├── DocumentActionsSheet.tsx
│   │   ├── DocumentDetailSheet.tsx
│   │   └── NotificationsScreen.tsx
│   └── utils/
│       ├── profile.ts            # UserProfile тип + loadProfile/saveProfile
│       ├── pdfGenerator.ts       # HTML-шаблон PDF + share
│       └── storage/              # обгортка над AsyncStorage
└── assets/
    ├── fonts/                    # Inter
    └── images/                   # bell.png, fab.png, trident.png, tab_*.png, test_photo.png …
```

### Дизайн-канва

- Канва — **1290px** (DESIGN_WIDTH). Усі розміри в коді задані в дизайн-юнітах.
- Скейл-функція `s(v) = v * (W / 1290)`, де `W = min(window.width, 430)`.
- 8-pt grid (8 / 16 / 24 / 32 в логічних пікселях, що приблизно = 24/48/72/96 в дизайн-юнітах).

---

## 4. Модель даних `UserProfile`

Файл `/app/frontend/src/utils/profile.ts`. Зберігається повністю серіалізованим JSON у `AsyncStorage[rezerv_profile_v1]`.

```ts
type UserProfile = {
  // ----- Картка Резерв ID -----
  surname: string;          // ІВАНОВ
  name: string;             // ІВАН
  patronymic: string;       // Іванович
  birthDate: string;        // ДД.ММ.РРРР
  deferralUntil: string;    // напр. "08.01.2027" або "завершення мобілізації"
  customTicker: string;     // власний текст бігучої стрічки (пусто = авто з часом)
  photoBase64: string|null; // data:image/jpeg;base64,...
  noPhoto?: boolean;        // якщо true — фото-блок ховається, інший layout картки

  // ----- PDF: ідентифікація -----
  validUntil: string;       // Дійсний до * (30.05.2027)
  rnokpp: string;           // 3411511010
  status: string;           // Військовозобов'язаний

  // ----- PDF: військова інформація -----
  category: string;         // Категорія обліку
  tck: string;              // ТЦК та СП (повна назва підрозділу)
  rank: string;             // Звання — Солдат
  registryNumber: string;   // Номер в реєстрі «Оберіг»
  vos: string;              // ВОС — 999097
  note: string;             // Примітка / опис підготовки
  deferralType: string;     // п.4 ч.1 ст.23

  // ----- PDF: адреса та контакти -----
  address: string;
  email: string;
  phone: string;
  dataUpdateDate: string;   // 21.05.2026
};
```

Усі додаткові поля для розширеного PDF (Постанова ВЛК, Група інвалідності тощо) **планується додати** як опціональні (див. §7).

---

## 5. Поточний стан UI

### 5.1 Головний екран (`app/index.tsx`)

**Структура (зверху вниз):**

1. `paddingTop: s(270)` — верхній відступ до плитки «Сповіщення».
2. Плитка «Сповіщення» (праворуч): `height: s(96)`, `borderRadius: s(100)`, текст `s(40)`, іконка дзвоника `s(58)x s(58)`.
3. `paddingTop: s(543)` — відступ до Резерв ID картки.
4. Картка `s(1146) x s(1542)`, `borderRadius: s(48)`:
   - **Лицьова сторона**: «Резерв ID» (s(64), Inter SemiBold), тризуб (s(120)x s(150)), фото (s(468)x s(624)), праворуч — Дата народження + Відстрочка до. Бігуча стрічка `top: s(1031)` (або `s(980)` для noPhoto-режиму). Знизу — статус «Військовозобов'язаний», ПІБ у три рядки, помаранчевий FAB-плюс `s(180)x s(180)`.
   - **Зворот**: «QR-код дійсний до …» + великий QR `s(980)`.
5. Tab-бар: 4 таби, висота `s(96)+s(36)+padding`.

### 5.2 Едит-режим

- Тап на тризуб → `editMode = true`.
- Поля стають `<TextInput>` (всі — surname/name/patronymic/birthDate/deferralUntil/customTicker, плюс фото міняється через `expo-image-picker`).
- FAB замінюється на круглу помаранчеву кнопку «OK» → `saveEdit()` → `saveProfile()` у AsyncStorage.

### 5.3 Settings (`app/settings.tsx`)

Окремий екран із повним редагуванням **усіх** полів `UserProfile` (включно з полями для PDF: РНОКПП, ТЦК, ВОС, адреса, email, телефон тощо). Зміни негайно зберігаються в AsyncStorage та підтягуються на головному екрані через `useFocusEffect`.

### 5.4 Дочірні екрани

- **Сповіщення** — відкривається з плитки.
- **Сервіси → Редагувати дані онлайн** — заглушка.
- **Вакансії → Підтримка** — заглушка.
- **Меню → Активні сесії / Підтримка / Налаштування** — `Налаштування` веде у `/settings`.

---

## 6. PDF — точна структура цільового шаблону

Файл `eVOD_2026_05_30_18_46_04.pdf` має **одноколонковий** макет із наступним порядком зверху вниз:

### Header
- Зліва: `Резерв +` + `Військово-обліковий документ` + `Сформовано: ДД.ММ.РРРР, ГГ:ХХ`
- Справа: щит + «Міністерство оборони України»

### Identity Block
- `Військовозобов'язаний` (статус-бейдж)
- `ПІБ` — повне ім'я великим шрифтом
- Поряд (праворуч) — **QR-код** документа

### Сітка label/value (рядки з підкресленням), у такому порядку:
1. `Дата народження` — `27.05.1993`
2. `Дійсний до *` — `30.05.2027`
3. `РНОКПП` — `3411511010`
4. `Категорія обліку` — `Військовозобов'язаний`
5. `Підстава зняття виключення` — `-`
6. `ТЦК та СП` — `Білоцерківський районний…`
7. `Звання` — `Солдат`
8. `Номер в реєстрі Оберіг` — `250220231362300200481`
9. `BOC` — `999097`
10. `Потребує проходження базової загальновійськової підготовки, Солдат резерву` *(заголовок-блок без значення)*
11. `Тип відстрочки:` — `п.4 ч.1 ст.23`
12. `Відстрочка до` — `завершення мобілізації`
13. `Причина звернення до Нацполіції` — `-`
14. `Дата звернення:` — `-`
15. `Постанова ВЛК` — `-`
16. `Дата ВЛК` — `-`
17. `Група інвалідності` — `-`
18. `Діє до` — `-`
19. `Причина інвалідності` — `-`
20. `Адреса проживання` — повний рядок адреси
21. `Email` — `shillclub.dao@gmail.com`
22. `Телефон` — `380952067447`
23. `Дата уточнення даних` — `21.05.2026`

### Footer
> *Документ дійсний до зазначеної на ньому дати. Якщо вказані в ньому дані змінюються в Єдиному державному реєстрі призовників, військовозобов'язаних і резервістів «Оберіг», документ втрачає чинність. Завантажуйте мобільний застосунок Резерв+ та користуйтеся завжди актуальним електронним документом.*

> **Фото у PDF відсутнє.** Тільки QR.

---

## 7. Невирішені / в роботі задачі (Backlog)

### 🔴 P0 — критичні (наступний крок)

1. **Переписати `pdfGenerator.ts` під одноколонковий шаблон.**
   - Поточний HTML — 2-колонковий, що **не відповідає** оригіналу.
   - Видалити `.grid-2col`, `.col`. Залишити простий вертикальний потік `.row` (label + value) з підкресленням знизу.
   - Додати поля у `UserProfile`: `removalBasis`, `policeReason`, `policeDate`, `vlkDecision`, `vlkDate`, `disabilityGroup`, `disabilityValidUntil`, `disabilityReason`. Усі за замовчуванням `"-"`.
   - QR-код розмістити в правому верхньому куті (поряд із ПІБ), без фото.
   - Header: бренд `Резерв +` + центр (`Військово-обліковий документ`, `Сформовано: …`) + правий блок (Міністерство).
   - Footer — точний текст з §6.

2. **Підтвердити одиниці вимірювання у скрін-специфікації Figma.**
   - Зараз: `bell = s(58)`, `fab = s(180)` (дизайн-юніти, рендеряться як ~19px і ~60px).
   - Якщо в Figma «120x120» та «58» — це **логічні пікселі**, треба збільшити до `s(360)` та `s(174)` відповідно.
   - **Очікую підтвердження від користувача** перед коригуванням.

3. **Тестування пайплайну: профіль → PDF.**
   - Зайти у `/settings`, змінити кілька полів, повернутись на головну, згенерувати PDF, переконатися що значення з AsyncStorage реально потрапили у файл.

### 🟠 P1 — важливі

4. **Settings UX**: групувати поля в секції з заголовками («Особисті дані», «Військова інформація», «Адреса та контакти», «PDF: розширені поля»). Додати валідацію формату дат.
5. **Settings → Очистити дані**: кнопка «Скинути до DEFAULT_PROFILE» з підтвердженням.
6. **Edit mode на картці**: додати індикатор збереження / `Alert` про успіх.
7. **QR-код**: у поточному UI використовується JSON-payload. Зробити його стабільним і людино-читаним опціонально (deeplink або підписаний JWT-токен).
8. **Тікер-стрічка**: при `customTicker` показувати збоку маркер «(власний)».

### 🟡 P2 — покращення

9. **Темна тема** (опціонально).
10. **Локалізація**: винести усі рядки у `i18n` (`en/uk`).
11. **Експорт/Імпорт профілю**: JSON-файл через `expo-file-system` + `expo-sharing`.
12. **Підтримка кількох карток / профілів**.
13. **Анімація переходу між табами**.
14. **Більше скрин-стейтів**: «Помилка» / «Завантаження» / «Empty state» у дочірніх екранах.

### ⚪ Технічний борг

15. Рознести `index.tsx` (~1245 рядків) на окремі модулі: `FrontSide.tsx`, `BackSide.tsx`, `TabBar.tsx`, `useTickerAnimation.ts`.
16. Стилі: винести `labelStyle/valueStyle/tickerStyle/…` у `/src/styles/cardStyles.ts`.
17. Покрити юніт-тестами `profile.ts`, `pdfGenerator.ts → buildHtml()`.
18. Додати ESLint правило `no-magic-numbers` для дизайн-юнітів та використовувати named-константи.

---

## 8. Як продовжити роботу — крок за кроком

```text
КРОК 1: уточнити у користувача одиниці виміру для FAB (120) та bell (58).
КРОК 2: переписати pdfGenerator.ts → 1-колонковий шаблон з §6.
КРОК 3: розширити UserProfile додатковими полями (ВЛК, інвалідність, поліція).
КРОК 4: оновити settings.tsx, додавши нові секції.
КРОК 5: візуально перевірити PDF на macOS preview / Acrobat — порівняти зі скрином оригіналу.
КРОК 6: запустити testing_agent (frontend) — перевірка edit-flow, persistence, PDF-share.
КРОК 7: рефакторинг index.tsx (P2) — лише після стабілізації P0.
```

---

## 9. Команди розробки

```bash
# Frontend
sudo supervisorctl restart expo

# Backend (наразі не використовується для бізнес-логіки)
sudo supervisorctl restart backend

# Логи
tail -n 200 /var/log/supervisor/expo.*.log

# Скріншот
# через mcp_screenshot_tool на http://localhost:3000
```

---

## 10. Контракти API

**Жодного зовнішнього API** не використовується. Уся персистентність — `AsyncStorage`. Це принципово, щоб гарантувати offline-first роботу.

---

## 11. Відомі обмеження

1. **PDF-генерація на Web** використовує `window.print()` — не зберігає файл, а відкриває діалог друку. На iOS/Android — повний `expo-print → expo-sharing`.
2. **Фото** зберігається як base64 у AsyncStorage → обмеження ~6MB на значення. Тому фото стискається до `quality: 0.7` і `aspect: [3,4]` при виборі.
3. **QR-код** містить лише компактний JSON — у реальному застосунку має бути крипто-підписаний токен від МО.
4. **Web preview** через Metro може не відображати `expo-image-picker` коректно — тестувати на пристрої через Expo Go.

---

## 12. Чек-лист для нового агента

- [ ] Прочитати **цей файл повністю**.
- [ ] Прочитати `/app/frontend/src/utils/profile.ts` та `/app/frontend/src/utils/pdfGenerator.ts`.
- [ ] Завантажити шаблон `eVOD_2026_05_30_18_46_04.pdf` (URL у §6) та порівняти візуально з поточним PDF.
- [ ] Уточнити у користувача питання з §7 P0-2 (одиниці виміру).
- [ ] Реалізувати §7 P0-1 (1-колонковий PDF).
- [ ] Прогнати `testing_agent` (frontend) перед фіналізацією.

---

*Документ підтримується актуальним. Останнє оновлення: 30 травня 2026 р.*
